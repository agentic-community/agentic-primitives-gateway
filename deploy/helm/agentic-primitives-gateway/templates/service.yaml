apiVersion: v1
kind: Service
metadata:
  name: {{ include "agentic-primitives-gateway.fullname" . }}
  labels:
    {{- include "agentic-primitives-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agentic-primitives-gateway.selectorLabels" . | nindent 4 }}
